# Lab 6 reference

Start with the complete single-job workflow example. Then move validation into
the supplied workflow_call example and call it with root: module. Keep read-only
permissions, mocked tests and allowlisted paths. A failed invalid-CIDR run must
be corrected rather than skipped. Promotion reuses logic, never a saved dev plan.
