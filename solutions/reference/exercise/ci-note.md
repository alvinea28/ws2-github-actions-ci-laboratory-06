# Explain your actual experiment

An invalid CIDR at var.address_space (10.300.0.0/16) must fail the intended
valid-topology test. Restore 10.42.0.0/16 and verify the same check passes.
Record your real run results; this example is not historical failure evidence.
